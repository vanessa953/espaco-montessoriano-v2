import Layout from '../components/Layout'

export default function Financeiro() {
  return (
    <Layout title="Financeiro" subtitle="Previsões, receitas, despesas e fechamento mensal">
      <div className="panel">
        <div className="panelHead">
          <h2>Financeiro</h2>
          <button className="primary">Novo registro</button>
        </div>
        <div className="formGrid">
          <label>Paciente<input placeholder="Nome do paciente" /></label>
<label>Serviço<input placeholder="Tipo de atendimento" /></label>
<label>Valor<input placeholder="R$ 0,00" /></label>
<label>Data prevista<input placeholder="dd/mm/aaaa" /></label>
<label>Status<input placeholder="Pendente/Pago" /></label>
<label>Observações<input placeholder="Detalhes" /></label>
        </div>
      </div>

      <div className="panel">
        <h2>Registros</h2>
        <p className="empty">Nenhum registro cadastrado ainda. Estrutura preparada para conexão com Supabase.</p>
      </div>
    </Layout>
  )
}
